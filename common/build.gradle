apply from: '../common_lib.gradle'

android {
    defaultConfig {
        versionCode 1
        versionName "1.0.0"
    }

    buildTypes {
        debug {
            minifyEnabled false
            buildConfigField "String", "ROUTE_ROOT", ROUTE_ROOT
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        release {
            minifyEnabled false
            buildConfigField "String", "ROUTE_ROOT", ROUTE_ROOT
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    api fileTree(include: ['*.jar'], dir: 'libs')
    implementation project(":base")

    api libraries.autoSize
    api libraries.immersionBar
    api libraries.immersionBar_kotlin

    api libraries.arouterApi
    api libraries.rxPermission
    api libraries.glide
    api libraries.gson
}
