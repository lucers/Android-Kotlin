apply from: '../common_build.gradle'

android {
    defaultConfig {
        if (isRunAlone.toBoolean()) {
            applicationId "com.lucers.widget"
        }
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}
